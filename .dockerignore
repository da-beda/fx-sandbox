# The reference image copies no repository files. Deny the build context by
# default so raw repository builds cannot send source, .env files, credentials,
# or optional experiment state to the Docker daemon/builder.
**
!Dockerfile
!.dockerignore
